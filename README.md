# Ice-Crawler
ICE CRAWLER — Triadic Zero-Trace Repository Ingestion. Phase-isolated pipeline (Frost → Glacier → Crystal) that produces deterministic, hashable artifacts for AI/code analysis with guaranteed teardown and zero persistent residue.
